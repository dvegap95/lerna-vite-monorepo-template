import ViewTestObject from '@/common/__tests__/View.to';
import { ___FTName|constantcase____PATH } from '@/common/config/routes';

export default class ___FTName___TestObject extends ViewTestObject {
  async render() {
    await this.app.render({
      route: ___FTName|constantcase____PATH,
    });
    return this;
  }
}
